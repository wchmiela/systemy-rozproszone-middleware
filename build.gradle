plugins {
    id 'java'

    id 'application'
}

mainClassName = 'Application'

dependencies {
    compile group: 'com.google.protobuf', name: 'protobuf-java', version: '3.5.1'
}

repositories {
    jcenter()
    mavenCentral()
    mavenLocal()
}
